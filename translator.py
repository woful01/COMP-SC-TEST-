def reverseLookup(data,value):
    keys[]
    for key in data:
        if data[key]==value:
            keys.append(key)
    return
def main():
    frEn={"le":"the","la":"the","livre":"book","pomme":"apple"}
    print("the french word for 'the' are:",reverseLoop(frEn,"the"))
    print()







